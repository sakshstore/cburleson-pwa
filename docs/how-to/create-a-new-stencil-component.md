`npm run stencil generate <component-name (e.g. app-mycomponent)>`

or...

`stencil generate <component-name>` (if stencil is installed globally) 

`npx stencil generate <component-name>` (if stencil is not installed globally)